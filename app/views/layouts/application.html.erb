<!DOCTYPE html>
<html>
<head>
  <title>VidyaGaem</title>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="indigo lighten-4">
	
    <nav class="deep-purple darken-4" role="navigation">

		<div class="nav-wrapper container">
			<a id="logo-container" href="#" class="brand-logo">AFK RPG</a>
			<ul class="right">

			<% if current_user %>
				<li>Welcome back, <%= current_user.email %>, you loser.</li> 
				
				<li><%= button_to("Sign Out", destroy_user_session_path, method: :delete, class: "btn red darken-3 white-text") %></li>
			<% else %>
				<li>
					You must be new here.
					<%= link_to("Sign In", new_user_session_path, class: "btn blue darken-3 white-text") %>
					<%= link_to("Sign Up", new_user_registration_path, class: "btn blue darken-3 white-text") %> 
				</li>
			<% end %>
			</ul>
		</div>

	</nav>
	
	
	<div class="container">
			<% if current_user %>
				<div class="card-panel purple lighten-3">
					<%= link_to("Units", units_path, class: "btn purple darken-3 white-text") %>
				</div>
			<% end %>
			<% if notice || alert %>
				<div class="card-panel deep-purple lighten-4">
					<p class="notice"><%= notice %></p>
					<p class="alert"><%= alert %></p>
				</div>
			<% end %>
			
			
			<div class="card-panel blue lighten-5">
				<%= yield %>
			</div>
	</div>
    
</body>
</html>
